# Server
PORT=3001
NODE_ENV=development

# SSL/TLS Configuration (required in production)
# Option 1: Use Let's Encrypt (recommended - auto-detects certificates)
# Set your domain name and the service will automatically find certificates at:
# /etc/letsencrypt/live/<domain>/fullchain.pem
# /etc/letsencrypt/live/<domain>/privkey.pem
LETSENCRYPT_DOMAIN=

# Option 2: Manual certificate paths (if not using Let's Encrypt)
SSL_CERT_PATH=
SSL_KEY_PATH=
# Optional: CA certificate for client certificate validation
SSL_CA_PATH=

# Option 3: Reverse proxy mode
# Set to "true" if running behind a reverse proxy (nginx, traefik, Caddy) with SSL termination
# WARNING: Only use this if your proxy handles HTTPS - the server will start without SSL
TRUST_PROXY=false

# CORS
CORS_ORIGIN=http://localhost:5173

# Data directory (for SQLite database)
DATA_DIR=./data

# Google OAuth
# Get from: Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# Web UI (built React app path)
WEB_DIST_PATH=../web/dist

# Push Notifications (VAPID keys)
# Leave empty or with placeholders - keys will be auto-generated on first run
# The generated keys will be printed to console - copy them here for persistence
# Or manually generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@brewos.io
